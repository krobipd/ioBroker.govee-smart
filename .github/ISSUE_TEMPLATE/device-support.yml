name: Geräte-Support melden / Report device support
description: Erfahrung mit einem bisher experimentellen oder nicht gelisteten Govee-Gerät teilen / Share experience with an experimental or unlisted Govee device
title: "[Gerät] H???? — kurz beschreiben"
labels: ["device-support"]
body:
  - type: markdown
    attributes:
      value: |
        Danke für deinen Bericht! Diese Vorlage hilft uns, dein Gerät auf 🟢 **User-bestätigt** oder ✅ **Voll getestet** zu promoten.

        Thanks for the report! This template helps us promote your device to 🟢 **User-confirmed** or ✅ **Fully tested**.

  - type: input
    id: sku
    attributes:
      label: SKU / Modell (optional)
      description: Steht auf dem Gerät und in der Govee Home App, z.B. `H7160`. Steckt auch im Diagnose-Export. / On the device and in the Govee Home app. Also part of the diagnostics export.
      placeholder: H7160

  - type: dropdown
    id: status-current
    attributes:
      label: Aktueller Status im Wiki (optional) / Current Wiki status (optional)
      options:
        - "⚪ Experimentell / Experimental"
        - "🟢 User-bestätigt / User-confirmed (Folge-Bestätigung)"
        - "Nicht gelistet / Not listed yet"

  - type: textarea
    id: behavior
    attributes:
      label: Was hast du probiert? Was funktioniert? Was nicht?
      description: |
        Konkret, kurz, ehrlich. Was geht? Was geht nicht? Hattest du Fehler im Adapter-Log?

        Concrete, brief, honest. What works? What doesn't? Any errors in the adapter log?
      placeholder: |
        - Power on/off: works / funktioniert
        - Change mode / Modus wechseln: works / funktioniert
        - Set temperature / Temperatur setzen: no effect on the device / kein Effekt am Gerät
    validations:
      required: true

  - type: textarea
    id: diagnostics
    attributes:
      label: Diagnose-Export / Diagnostics export
      description: |
        In der Instanz-Konfiguration: Reiter **Experte** → **Diagnose** → Gerät wählen → **Bericht erstellen und herunterladen**, dann die heruntergeladene Datei hier hineinziehen. Der Bericht enthält auch die letzten Protokollzeilen und ist anonymisiert.

        In the instance settings: tab **Expert** → **Diagnostics** → pick the device → **Create report and download**, then drag the downloaded file in here. The report also contains the most recent log lines and is anonymised.
    validations:
      required: true

  - type: input
    id: adapter-version
    attributes:
      label: govee-smart Version
      description: Reiter Instanzen / Instances tab
      placeholder: "x.y.z"
    validations:
      required: true

  - type: textarea
    id: extras
    attributes:
      label: Sonstiges (optional)
      description: Firmware-Version, ungewöhnliche Setup-Details, weitere Beobachtungen / Firmware version, unusual setup, additional observations
