# macOS
.DS_Store

# Node.js
node_modules/
npm-debug.log*

# IDE
.idea/
.vscode/*
!.vscode/settings.json
*.swp
*.swo

# ioBroker repochecker
.commitinfo

# ioBroker
admin/i18n/flat.txt

# Build & Test
coverage/
.nyc_output/
*.tsbuildinfo

# Admin custom component: the BUILT admin/custom/ is tracked (like build/) so a GitHub install
# gets the component too — it is not built during a GitHub install, only on npm prepublish.
# Only the src-admin work dirs stay ignored.
/src-admin/build
/src-admin/node_modules
/src-admin/package-lock.json

# Packages
*.tgz
package/

# Dev server
.dev-server/

# Environment
.env
.env.local

# Claude Code local files (not published)
.claude/settings.local.json
.claude/dev-history.md
.claude/plans/
CLAUDE.local.md
# Internal planning/spec artifacts (brainstorming output — not for the public repo)
docs/superpowers/
